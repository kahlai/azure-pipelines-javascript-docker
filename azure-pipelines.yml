trigger:
- main

pool:
  vmImage: 'ubuntu-latest' 

variables:
  imageName: 'kahlai/pipelines-javascript-docker'

steps:
- task: Docker@2
  displayName: Build an image and push
  inputs:
    containerRegistry: 'Quay'
    repository: $(imageName)
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
- checkout: self
  persistCredentials: true
- bash: |
    echo 'Hello World'
    echo $(PAT)
    git config --global user.name "kahlai"
    git config credential.username "kahlai"
    git config --global user.email "kahlai@redhat.com"
    git clone https://kahlai:$(PAT)@github.com/kahlai/azure-pipeline-demo.git
    cd azure-pipeline-demo
    touch README.md
    git add README.md
    echo $(PAT)
    git commit -m "first commit"
    git push origin
# - task: GitHubRelease@1
#   inputs:
#     gitHubConnection: 'Github'
#     repositoryName: '$(Build.Repository.Name)'
#     action: 'create'
#     target: '$(Build.SourceVersion)'
#     tagSource: 'gitTag'
#     changeLogCompareToRelease: 'lastFullRelease'
#     changeLogType: 'commitBased'
    